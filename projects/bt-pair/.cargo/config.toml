[build]
# Default target for this project (comment out for desktop preview)
# target = "armv7-unknown-linux-gnueabihf"

[target.armv7-unknown-linux-gnueabihf]
# Linker is set via CARGO_TARGET_ARMV7_UNKNOWN_LINUX_GNUEABIHF_LINKER env var in shell.nix
