<!DOCTYPE busconfig PUBLIC "-//freedesktop//DTD D-BUS Bus Configuration 1.0//EN"
 "http://www.freedesktop.org/standards/dbus/1.0/busconfig.dtd">
<busconfig>
  <!-- ble-ssh-watch: Allow our daemon to talk to BlueZ -->
  <policy user="root">
    <allow own="com.ble_ssh"/>
    <allow send_destination="org.bluez"/>
    <!-- Allow BlueZ to call back into our GATT application via unique bus name.
         BlueZ calls ObjectManager, Properties, and GATT interfaces on our app
         to serve the GATT services to remote BLE clients. -->
    <allow send_interface="org.freedesktop.DBus.ObjectManager"/>
    <allow send_interface="org.freedesktop.DBus.Properties"/>
    <allow send_interface="org.freedesktop.DBus.Introspectable"/>
    <allow send_interface="org.bluez.GattService1"/>
    <allow send_interface="org.bluez.GattCharacteristic1"/>
    <allow send_interface="org.bluez.GattDescriptor1"/>
    <allow send_interface="org.bluez.LEAdvertisement1"/>
    <allow send_interface="org.bluez.Profile1"/>
  </policy>
</busconfig>
